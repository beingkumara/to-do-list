<!DOCTYPE html>
<html>

<head>
    <meta charset='utf-8'>
    <meta http-equiv='X-UA-Compatible' content='IE=edge'>
    <title>
        TO DO LIST
    </title>
    <meta name='viewport' content='width=device-width, initial-scale=1'>
    <link rel='stylesheet' href='styles.css'>
    <script src='main.js'></script>
</head>

<body>
    <div class="box" id="heading">
        <h1>
            <%= actualDay  %>
        </h1>
    </div>

    <!-- when we use control flow in th code, for each line of js we have to use <% %>
    that is why I used <% %> in the case of for loop -->
    <div class="box">
        <% for(var i =0; i< listItems.length ; i++){ %>

            <form action="/delete" method="post">
                <div class="item">
                    <input type="checkbox" name="checkedItem" value="<%= listItems[i]._id %>" onChange="this.form.submit()">

                    <p>
                        <%= listItems[i].name %>
                    </p>
                </div>
                <input type="hidden" name="deleteList" value="<%= actualDay %>"></<input>
            </form>
            <%}%>
                <div class="item">
                    <form class="item" action="/" method="post">
                        <input type="text" name="toDo" placeholder="New Item" autocomplete="off">
                        <button type="submit" name="button" value="<%=actualDay%>">+</button>
                    </form>
                </div>

    </div>
</body>

</html>